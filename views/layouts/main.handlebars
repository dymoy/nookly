<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nookly: Recycle Home Items</title>
    <!-- Link Bootstrap framework -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
    <header class="d-flex flex-direction-row justify-content-between align-items-center">
        <h1 class="my-auto">
            <a href="/" class="mx-4">nookly</a>
        </h1>
        <nav class="navbar mx-4">
            {{#if loggedIn}}
                <a href="/" class="navbar-item">Home</a>
                <a href="/profile" class="navbar-item">Profile</a>
                <a href="/" class="navbar-item" id="logout" type="button">Log out</a>
            {{else}}
                <a href="/" class="navbar-item">Home </a>
                <a href="/login" class="navbar-item">Profile</a>
                <a href="/login" class="navbar-item">Login</a>
            {{/if}}
        </nav>
    </header>
    <main class="container">
        {{{body}}}
    </main>
    <footer></footer>
    
    <!-- Render script for logged in users only -->
    {{#if loggedIn}}
        <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>
